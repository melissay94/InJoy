// CreatePrompt: Prompt
// EditPrompt: Prompt
// DeletePrompt: Boolean
// CreatePost: Post


// Prompt mutations
async function createPrompt(root, { titile, image, tips, category, posts }, { currentUser, models }) {
 // tbd
}
  
  module.exports = {
    createPrompt
  }